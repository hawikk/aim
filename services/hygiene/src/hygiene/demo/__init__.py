"""The seeded fixture repo the acceptance criteria are written against.

Ships inside the package so the demo is runnable from the container image:
    python -m hygiene.demo.seed_repo /tmp/seeded-repo
"""
