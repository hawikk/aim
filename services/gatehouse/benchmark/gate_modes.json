{
  "version": 1,
  "description": "Runtime mode per gate. enforce = findings may fail the check; observe = report only. Auto-written by the precision harness when a gate exceeds its FP budget.",
  "updated_at": null,
  "source_report": null,
  "gates": {
    "gitleaks": "enforce",
    "semgrep": "enforce",
    "checkov": "enforce",
    "trivy": "enforce"
  }
}
