"""First-class managed-config locations (AIM-1170).

The directory name is hyphenated so this is not an importable package.
Load ``paths.py`` via importlib (see ``scripts/aim-1170-macos-managed-user-proof.sh``).
Collectors inline the same candidate list so each package stays standalone.
"""
