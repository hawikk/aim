{
  "name": "@aim/audit",
  "version": "0.1.0",
  "private": true,
  "description": "Tamper-evident append-only audit trail (hash-chained, HMAC-sealed) for dashboard access, policy changes, and finding lifecycle",
  "type": "module",
  "exports": {
    ".": "./src/audit-log.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
