{
  "name": "@aim/alerting",
  "version": "0.1.0",
  "private": true,
  "description": "Guardrail finding forwarding to Microsoft Sentinel (Log Analytics ingestion) with severity mapping and runbook pointers",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./severity": "./src/severity.ts",
    "./cef": "./src/cef.ts",
    "./sentinel": "./src/sentinel.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test --experimental-strip-types test/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0"
  }
}
