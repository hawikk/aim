# Python
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
*.pyc

# Node
node_modules/
dist/
coverage/
*.tsbuildinfo

# Local data / scratch
*.db
ai-monitoring/  # nested pilot scratch repo — kept out of the platform repo

# Environment & secrets — never commit these
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx

# Terraform
**/.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
crash.*.log
.terraform.lock.hcl

# Scan / SBOM artifacts
sbom.json
sbom.*.json
gitleaks-report.json

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# Logs
*.log
logs/
# Except the synthetic proxy parser fixtures, which are .log by format.
# check_proxy_fixture_matrix.py --check goes red without them.
!collectors/proxy/samples/*.sample.log
.claude/

# Dogfood multi-host fleet state (device tokens) — never commit
deploy/dogfood/multi-host/state/

# Air-gap bundle output
deploy/airgap/out/
# Intune Win32 staging output
deploy/windows/out/

# `aim` CLI build artifacts: dist/ is already ignored
# globally; the intermediate staging tree and the vendored collector tree are
# not. `_vendor/` is regenerated from the collector source by
# scripts/build_aim_cli.py on every build — it is build output, never edited by
# hand, and must not be committed (drift risk against the real collectors).
packaging/build/
packaging/aim-cli/src/aim/_vendor/

# Machine-owned policy files written by the dashboard rule-tuning / alert
# config APIs — live config, not PR-reviewed policy.
policies/guardrail/v1/ui-overrides.yaml
policies/guardrail/v1/alerts.yaml

# GitHub App private key and any other local PEM/secret material.
deploy/secrets/*
!deploy/secrets/README.md

# local CI attestations are per-run evidence, not source
ci-local-attestation.json
evasion-report.json

# Headless UI captures — regenerate with apps/web npm run screenshot
apps/web/test/screenshots/

# Friend-preview basic-auth secret
apps/landing/deploy/.htpasswd

# Local image pin file for pilot pull path — copy from
# deploy/compose/images.pin.env.example; may hold site-specific digests.
deploy/compose/images.pin.env
